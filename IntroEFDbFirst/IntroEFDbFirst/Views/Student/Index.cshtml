@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@if (Model.Count != 0)
{
    <table class="table table-bordered">
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Cgpa</th>
            <th>Department</th>
            <th></th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Cgpa</td>
                <td>@item.DeptId</td>
                <td>
                    <a class="btn btn-primary" href="/student/details/@item.Id">Details</a>
                    <a class="btn btn-success" href="/student/edit/@item.Id">Edit</a>
                    <a class="btn btn-danger" href="/student/delete/@item.Id">Delete</a>
                </td>
            </tr>
        }
    </table>    

}
else
{
    <p class="alert alert-info">No Data Found</p>
}
